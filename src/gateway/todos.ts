import { TodoSession } from "@/interfaces/UserSession"
import { api } from "@/services/api"

export async function getTodos(columnId: string) {
  // const response = await api.get<TodoSession[]>(`/todos?idColumn=${columnId}`)
  // return response.data
  return
}
